#!/usr/bin/env rc

if(test $#* -ne '1'){
	echo 'gitpost action' >[1=2]
	exit 1
}

cd $HOME/proj

for(i in `{find -maxdepth 1 -type d | grep -v '^\.$'}){
	cd $i
	for(j in `{find -maxdepth 1 -type d | grep -v '^\.$'}){
		cd $j
		printf '%s: ' $j
		eval $1
		cd ..
	}
	cd ..
}
