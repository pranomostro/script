-write an alternative to closest which is better and wraps around the screen
-write 'space' that looks for the biggest free rectangle on the screen
-write a script that lets the user map and unmap window groups with lemonbar
