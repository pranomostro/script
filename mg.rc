#!/usr/bin/env rc

if(~ $1 '-t'){
	trust='1'
	shift
}

if(~ $#* 0){
	echo 'mg [-t] FILES'
	exit 1
}

nam=`{gen $#*}

for(c in `{seq 1 $#*}){
	if(test -e $nam($c)) -a trust != '1'){
		echo 'error: not replacing '$nam($c)', exiting' >[2=1]
		exit 2
	}
	mv $*($c) $nam($c)
}

exit 0
