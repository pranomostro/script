#!/usr/bin/env rc

if(test -z $"*){
	echo '9 program [args]' >[1=2]
	exit 1
}

a=$1; shift
test -x $a || echo 'error: '$1' is not an executable file in /usr/local/plan9/bin, exiting' >[1=2] && exit 1
exec '/usr/local/plan9/bin/'$a $*
