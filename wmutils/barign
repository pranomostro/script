#!/usr/bin/env rc

#wait until the bar has started
while(~ `{lsw -a | wc -l} 0)
	sleep 0.1

#ignore all bars
for(i in `{lsw -a}){
	if(~ `{wname $i } 'bar'){
		chwb -s 2 -c 0x6c71c4 $i
		ignw -s $i
	}
}
