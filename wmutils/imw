#!/usr/bin/env rc

switch($#*){
case '0'
	win=`{pfw}
case '1'
	win=$1
case *
	echo 'imw [WIN]' >[2=1]
	exit 1
}

if(~ $win ''){
	echo 'No window found, exiting' >[2=1]
	exit 2
}

if(~ `{lsw | grep -c '^'$win'$'} '0'){
	echo 'no'
	exit 0
}

echo 'yes'
